
"""
def branch_and_bound():
    v = root()
    h = maxheap()
    best = value(v)
    insert(H, v)
    while heap:
        remove(H, v)
        if bound(v) is better than best:
            for each child u in v:
                if value(u) is better than best:
                    best = value(u)
                if bound(u) is better than best:
                    insert(H, u)
"""

